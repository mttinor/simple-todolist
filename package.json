{
  "name": "simple-todolist",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd front && npm run dev",
    "install:all": "cd backend && npm install && cd ../front && npm install"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
